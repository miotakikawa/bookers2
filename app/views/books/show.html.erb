<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">

     <h2>User info</h2>

      <table class="table table-hover table-inverse">
       <tr>
        <%=link_to user_path(@book.user) do %>
        <%= attachment_image_tag @book.user, :profile_image, :fill, 63, 63, fallback: "NO_IMAGE.jpeg", size:'63x63' %>
        <% end %>
        </tr>
       <tr>
	      <th>name</th>
	      <th><%= @book.user.name %></th>
       </tr>
       <tr>
        <th>introduction</th>
        <th><%= @book.user.introduction %></th>
       </tr>
       </table>


    <div class="row">
     <%=link_to "", edit_user_path(@book.user), class: "btn btn-outline-secondary btn-block fas fa-user-cog edit_user" %></a>
    </div>

    <h2 class="mt-3">New book</h2>
    <%= form_with model: @newbook, url:books_path, local:true do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>opionion</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Create Book', class: "btn btn-success" %>
    <% end %>

    </div>

  <div class="col-md-8 offset-md-1">
  <h2>Book detail</h2>
  <table class="table">
  <tbody>
  <tr>
　 <tbody>
　 <tr>
　  <td><span><%= attachment_image_tag @book.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg", size:'40x40' %></span><br>
　  <%=link_to @book.user.name, user_path(@book.user) %></td>
  　<td><span><%=link_to @book.title,book_path(@book)%></span></td>
 　  <td><span><%=@book.body%></span></td>
 　<% if @book.user == current_user %>
 　<td><span><%= link_to "Edit", edit_book_path(@book), class: "btn btn-sm btn-success" %></span>
 　</td>
 　 <td><span><%= link_to" Destroy", book_path(@book), method: :delete, class: "btn btn-sm btn-danger" %></span></td>
 　<%end%>
　</tr>
　</tbody>
  </table>
  </div>
 </div>
</div>